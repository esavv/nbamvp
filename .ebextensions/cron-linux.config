files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      */5 * * * * root cd /var/app/current && python3.11 predict_mvp.py --mode 'dev' >> /var/log/job1.log 2>&1 

commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/mycron.bak"